/**
 * @file config.ts
 * Identity shared by every Morpho version — v1, and any v2 that follows.
 */

/** Normalized provider id — the `products.provider` column value. */
export const MORPHO_PROVIDER = 'morpho'
