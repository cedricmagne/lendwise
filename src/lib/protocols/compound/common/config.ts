/**
 * @file config.ts
 * Identity shared by every Compound version — v3, and any v4 that follows.
 */

/** Normalized provider id — the `products.provider` column value. */
export const COMPOUND_PROVIDER = 'compound'
